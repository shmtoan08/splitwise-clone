"use client";
// TODO: Form nhập Expense — điều phối 3 kiểu chia (EvenSplit, CustomAmountSplit, SharesSplit)
// Không chứa logic tính toán — chỉ thu thập input rồi gọi addExpense() Server Action
export default function ExpenseForm() {
  return <div>ExpenseForm — TODO</div>;
}
